version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:current
    resource_class: small
    steps:
      - checkout
      - run:
          name: "Building"
          command: "echo 'Building'"
  test:
    docker:
      - image: cimg/base:current
    resource_class: small
    steps:
      - checkout
      - run:
          name: "Testing"
          command: "echo 'Testing'"

workflows:
  datashield_ci:
    jobs:
      - build
      - test
